class Hero{

    //properties
    char name[100];
    int health;
    int level;
};

// We can definitely define a class in a different file and then include it in your main program.
// In C++, it's a common practice to define the class in separate files to organize your code and make
//  it easier to manage, especially for larger projects.

// #include "hero.cpp"
